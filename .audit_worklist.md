# AUDIT WORK-LIST

## CONFIRMED P0/P1 (17)

### [1] P1 AI-generated practice re-banked under wrong subject (subject-mislabel bug confirmed)
- area: 题库 question bank + AI review generation
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/QuestionBank/QuestionBankView.swift @ GeneratedPracticeSheet.practiceCard line 282: `let subject = model.lastSeedSubjects.first ?? .general` (paired with QuestionBankModel.generatePractice lines 57-70 and QuestionBankModel.bankGenerated lines 84-100)
- category: ai-wiring | userRequested: True
- evidence: generatePractice iterates lastSeedSubjects and appends every subject's problems into ONE flat `generated` array (lines 57-70). GeneratedProblem (IntelligenceDTOs.swift lines 188-200) carries only `knowledgePointID`, no `subject`. The sheet renders the flat array and computes `subject = model.lastSeedSubjects.first ?? .general` once (line 282), then bankGenerated stores that single subject for EVERY card (QuestionBankModel line 88: `item.subject = subject`). When a multi-subject selection is used (e.g. math + 语文), lastSeedSubjects = ['数学','语文'] sorted by displayName, so all generated 语文 problems get re-banked as 数学. This is the known 'all generated banked under first seed subject' defect.
- FIX: Carry the subject on the DTO so it can never be mislabeled downstream. Minimal, precise edits:

1. IntelligenceDTOs.swift (GeneratedProblem, lines 188-200): add `public var subject: Subject` and a parameter in init (place it before knowledgePointID, give no default so callers must set it, OR default to .general to avoid touching unrelated call sites — prefer no default and update the two providers):
   public var subject: Subject
   public init(id: String = UUID().uuidString, subject: Subject, question: String, answer: String, steps: [SolutionStep] = [], difficulty: Int = 2, knowledgePointID: String = "") { ...; self.subject = subject; ... }

2. MockIntelligenceService.swift similarProblems (line 147): MockContent.similar already receives `subject: request.subject` — ensure MockContent.similar stamps each returned GeneratedProblem with that subject (set `subject: subject` in the GeneratedProblem(...) it builds).

3. CloudIntelligenceService.swift similarProblems (lines 362-368): set `subject: request.subject` on each mapped GeneratedProblem(...). (FoundationModelsService.swift:62-64 just forwards to fallback, so no change needed.)

4. QuestionBankView.swift practiceCard (line 282): delete `let subject = model.lastSeedSubjects.first ?? .general` and at the call site (line 319) pass `problem.subject`:
   model.bankGenerated(problem, subject: problem.subject, context: modelContext)
   Optionally also show the per-problem subject chip in the card.

5. QuestionBankModel.swift: keep bankGenerated's signature (still takes the subject) but it now receives the correct per-problem value. `lastSeedSubjects` can stay for any other use, but it is no longer load-bearing for banking; if unused elsewhere it may be removed.

Lower-effort alternative without the DTO change: in generatePractice build a `[String: Subject]` map (problem.id -> subject) while iterating, expose it on the model, and have practiceCard look up `subjectByProblemID[problem.id] ?? .general`. The DTO change is preferred because GeneratedProblem then mirrors SolvedProblem (which already carries `subject`) and removes the fragile sorted-first heuristic entirely.

### [2] P1 错题本 cannot save mistakes into the 题库 — required save-to-bank surface missing
- area: 题库 question bank + AI review generation
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Knowledge/MistakeNotebook/MistakeNotebookView.swift @ MistakeNotebookView (whole file) and MistakeDetailView.swift lines 110-118 (secondary actions: 重新讲解 / 举一反三 only)
- category: missing-feature | userRequested: True
- evidence: The user requirement is to save wrong problems to the bank from EVERY relevant surface including 错题本. Grepping the MistakeNotebook feature for BankedQuestion / 加入题库 / 智能出题 returns nothing. MistakeNotebookView only offers 组卷→MistakePaperSheet (its own paper), and MistakeDetailView's only secondary actions are 重新讲解 (tutor) and 举一反三 (router.openDrill). There is no 'add to 题库' on either the list, the multi-select bar, or the detail view, so mistakes can never feed the bank's 智能出题 loop. The two review systems (错题本 with forgetting-curve review, and 题库 with AI generation) are completely disconnected.
- FIX: Add an 加入题库 action that constructs a BankedQuestion from a MistakeItem and inserts it via modelContext.saveLogging(), mirroring WorkbookResultView.saveToBank / SolveResultView.addToBank. Two surfaces:

A) MistakeDetailView.swift (single item) — add a third secondary action alongside 重新讲解/举一反三 (lines 111-118), with a @State savedToBank: Set<UUID> guard so it shows 已加入题库 once tapped. Builder (note MistakeItem field names differ from GradedQuestion — there is NO `type`, and the cause field is `errorReason`, mapped to BankedQuestion.explanation):

  private func saveToBank(_ item: MistakeItem) {
      guard !savedToBank.contains(item.id) else { return }
      let q = BankedQuestion()
      q.subject = item.subject
      q.type = .other                                  // MistakeItem has no question-type field
      q.questionText = item.questionText
      q.imageData = item.imageData
      q.studentAnswer = item.studentAnswer
      q.correctAnswer = item.correctAnswer
      q.explanation = item.errorReason                 // 错因/解释
      q.errorType = item.errorType
      q.steps = item.steps
      q.setKnowledgePoints(item.knowledgePointIDs.map { KnowledgeRef(id: $0, name: knowledgePointName($0), subject: item.subject) })
      q.source = .mistake                              // the existing-but-unused case, finally produced
      q.mastery = item.mastery                         // carry over forgetting-curve progress
      q.nextReviewAt = item.nextReviewAt
      modelContext.insert(q)
      if modelContext.saveLogging() { savedToBank.insert(item.id); HapticEngine.play(.success) }
  }

knowledgePointName(_:) already exists in MistakeDetailView (lines 30-32), so names resolve correctly rather than persisting raw ids.

B) MistakeNotebookView.swift (bulk) — add a second button to paperBar (lines 141-151), e.g. 全部加入题库, iterating mistakes.filter { selectedIDs.contains($0.id) } and inserting one BankedQuestion each (factor the builder above into a shared static, ideally BankedQuestion.make(from: MistakeItem, knowledgePointName:) next to the existing make(from: GradedQuestion)). Guard duplicates with a savedToBank: Set<UUID>.

Verify on BOTH platforms: the action must be reachable and the 已加入题库 state must persist; afterward the question appears in QuestionBankView with the 错题本 source chip and is selectable as a seed for 智能出题, closing the loop the empty-state message at QuestionBankView.swift:78 already promises.

### [3] P1 题库 mastery/review-scheduling fields are dead — no review action, detail view, or due filter
- area: 题库 question bank + AI review generation
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/QuestionBank/QuestionBankView.swift @ QuestionBankView card() lines 142-200 (only toggleStar/delete mutate); BankedQuestion fields masteryRaw/reviewCount/nextReviewAt/lastReviewedAt in WorkbookModels.swift lines 130-134
- category: missing-feature | userRequested: True
- evidence: BankedQuestion persists mastery, reviewCount, nextReviewAt, lastReviewedAt and questions are seeded with nextReviewAt = +1 day, but the 题库 UI never updates them: the only mutations in QuestionBankView are toggleStar (line 221) and delete (line 227), and there is no question-detail route (grep for a bankDetail route returns nothing; AppDestinations maps .questionBank → QuestionBankView only). By contrast MistakeDetailView.review (MistakeDetailView.swift lines 161-175) implements a full ReviewGrade forgetting-curve flow advancing mastery and rescheduling nextReviewAt. The bank's review/mastery model is therefore non-functional — questions never become 'due', mastery never changes, and the spaced-repetition data is inert.
- FIX: Implement option (a) — the user-aligned complete review loop — reusing existing precedents so the change is minimal and consistent:

1) Add a review action inside QuestionBankView.card (or a lightweight inline review section / sheet keyed off the existing 看答案 reveal). When the answer is revealed, surface ReviewGrade buttons (ForEach(ReviewGrade.allCases)) exactly like MistakeDetailView.reviewSection (MistakeDetailView.swift:148-156). On tap call a new private review(_ q: BankedQuestion, _ grade: ReviewGrade) that mirrors MistakeDetailView.review (lines 161-175): set q.reviewCount += 1; q.lastReviewedAt = Date(); map grade -> mastery + interval days (again -> .weak/1, hard -> .developing/2, good -> reviewCount>=3 ? .mastered : .developing /4, easy -> .mastered/7); q.nextReviewAt = Calendar.current.date(byAdding: .day, value: days, to: Date()); modelContext.saveLogging(); HapticEngine.play(.success). This makes the seeded fields functional with no new model/schema work.

2) Add a '今日复习' (due-today) filter chip to filterBar (QuestionBankView.swift:123-140) gated on nextReviewAt <= now and mastery != .mastered, matching the mistakes precedent (ReviewFilter.dueToday in MistakeNotebookSupport.swift:103 and the filter in MistakeNotebookView.swift:33). Update the `filtered` computed property (lines 32-35) to honor it.

3) Optionally show a small due-state hint on each card (e.g. MistakePresentation.dueDescription(for: q.nextReviewAt) as already used at MistakeDetailView.swift:140) so the user can see when a question is due.

Do NOT take option (b) (deleting the fields) — the codebase deliberately seeds these fields from four call sites and clearly intends spaced-repetition for the bank, so completing the loop is correct; removing them would regress intent. Keep the change additive and confined to QuestionBankView.swift (+ optionally a tiny helper), which works identically on iOS and macOS since it reuses ReviewGrade/DBChip/DBCard that already render on both.

### [4] P1 Entire 模拟测验 (timed exam) feature is orphaned dead code — unreachable by users
- area: Drill / Exam / Essay practice
- file: Features/Exam/ExamModel.swift @ whole file; @MainActor final class ExamModel (line 147). No corresponding view exists in Features/Exam/.
- category: missing-feature | userRequested: True
- evidence: ExamModel.swift implements a full timed-exam flow (configure/assemble/runner navigation/tick countdown/submit/gradePaper/persistResults/addToMistakes/reset, lines 234-507) but a repo-wide grep for ExamModel, ExamView, ExamQuestion, 模拟测验, 交卷 outside ExamModel.swift returns ZERO hits. There is no ExamView.swift (Features/Exam/ contains only ExamModel.swift), no `case exam` in ToolKind (Models/AppEnums.swift:239-262 lists 23 tools, none is exam), and no exam destination in App/AppDestinations.swift. The feature — including its own 加入错题本 wiring (addToMistakes, line 468) — can never be opened, so all that logic ships nothing.
- FIX: Wire the existing model into navigation by building ExamView.swift; do not delete working, AI-integrated code that also satisfies the save-to-错题本 requirement. Minimal steps: (1) Models/AppEnums.swift — add `case exam` to ToolKind (after `.liveScan`), then add matching arms: displayName "模拟测验" (~line 290), symbolName "checklist" (~line 318), and a category arm in the `category` switch (~lines 324-333), e.g. group it under `.grade` alongside `.drill` (`case .gradeWorkbook, .gradeArithmetic, .gradeEssay, .drill, .exam: .grade`). Because both ToolKind.displayName/symbolName/category and AppDestinations.toolView are exhaustive switches, the project will not compile until every arm is added — which guarantees the tool gets surfaced (ToolsHubView/DBToolTile/SearchView all iterate ToolKind.allCases). (2) App/AppDestinations.swift — add `case .exam: ExamView()` to the toolView switch (after line 89). (3) Create Features/Exam/ExamView.swift mirroring DrillView's structure: a @State ExamModel, @Environment(\.intelligence), @Environment(\.modelContext), and SwiftData @Query for profile/masteries. Switch on model.phase: .setup (subject/题量/duration pickers calling model.configure(profileGrade:profileSubjects:) onAppear and model.assemble(using: intelligence) on start), .running (drive model.tick() once per second via a 1s Timer/TimelineView; show currentQuestion, currentAnswer binding, 上一题/下一题, 交卷→model.submit()), .timeUp (call model.gradeAfterTimeout() on appear), .graded (score ring, per-outcome ✓/✗ with revealed steps, 加入错题本 buttons calling model.addToMistakes(outcome, context:) gated on !model.hasSavedMistake(outcome), 再考一次→model.reset(); call model.persistResults(context:existingMasteries:) once on appear of the graded phase). No new build-target edits are needed — the project's synchronized root group auto-includes the new file. Deleting ExamModel.swift is the inferior alternative because it discards a complete, AI-wired, 错题本-integrated feature the user implicitly wants.

### [5] P1 Non-math drills are unwinnable in default (Mock) and on-device routes — every answer marked wrong
- area: Drill / Exam / Essay practice
- file: Services/Intelligence/MockContent.swift @ MockContent.similar(subject:count:knowledgePointID:) lines 97-113 (non-math branch line 108-110: answer = "见解析"); consumed by DrillModel.generate → DrillAnswerChecker.isCorrect (DrillModel.swift:304).
- category: ai-wiring | userRequested: True
- evidence: ContentCatalog.knowledgePoints includes Chinese (4), English (4) and Physics (1) points (verified), all offered in the drill subject/KP picker. For any non-math subject MockContent.similar returns GeneratedProblem(question: "请仿照例题…", answer: "见解析"). DrillAnswerChecker.isCorrect can only return true if the learner literally types '见解析' (no numeric/part/token path matches). MockIntelligenceService is the app default (IntelligenceService.swift:38) and FoundationModelsService.similarProblems delegates straight to the Mock fallback with no on-device generation (FoundationModelsService.swift:62-64), so BOTH default routes are affected. Result: every non-math drill question is graded wrong, the verdict banner shows '正确答案：见解析', and the EMA in persistResults (DrillModel.swift:363) drives the knowledge point's mastery DOWN for practicing.
- FIX: Make non-math mock drills genuinely answerable with deterministic, checkable items, so DrillAnswerChecker can pass for a correct learner answer and persistResults rewards mastery.

Minimal, contained fix in Services/Intelligence/MockContent.swift — replace the non-math else branch in `similar(subject:count:knowledgePointID:)` (lines 108-110) with a subject-aware single-choice item whose answer is a real label ("A".."D"). DrillAnswerChecker already accepts a bare token, and the math path stays untouched:

    } else {
        // Deterministic single-choice item with a real checkable answer (A..D),
        // so the learner can actually win and mastery updates meaningfully.
        let bank: [(q: String, choices: [String], correct: Int)]
        switch subject {
        case .chinese:
            bank = [("下列词语书写完全正确的一项是：", ["A. 一丝不苟  B. 再接再励  C. 不径而走  D. 走头无路"], 0),
                    ("「春风又绿江南岸」中「绿」字的妙处在于：", ["A. 用作动词，写出春风使江南变绿的动态  B. 单纯写颜色  C. 押韵需要  D. 没有特别含义"], 0)]
        case .english:
            bank = [("Choose the correct form: She ___ to school every day.", ["A. go  B. goes  C. going  D. gone"], 1),
                    ("Choose the correct word: I have ___ apple.", ["A. a  B. an  C. the  D. /"], 1)]
        case .physics:
            bank = [("一个静止在水平桌面上的物体，受到的合力是：", ["A. 重力  B. 支持力  C. 零  D. 摩擦力"], 2)]
        default:
            bank = [("关于「\(subject.displayName)」核心概念，正确的一项是：", ["A. 正确选项  B. 干扰项一  C. 干扰项二  D. 干扰项三"], 0)]
        }
        let pick = bank[i % bank.count]
        let labels = ["A", "B", "C", "D"]
        let answerLabel = labels[pick.correct]
        return GeneratedProblem(
            question: pick.q + "\n" + pick.choices.joined(separator: "\n") + "\n（请填写正确选项的字母）",
            answer: answerLabel,
            steps: [SolutionStep(index: 1, title: "解析",
                                 detail: "正确答案是 \(answerLabel)。结合\(approach(for: subject))")],
            difficulty: min(5, i + 1), knowledgePointID: knowledgePointID)
    }

Why this is correct and minimal:
- DrillAnswerChecker.isCorrect normalizes "A".."D" and matches the bare token, so typing the letter wins; it also provides a non-empty `steps` so the reveal shows a real explanation instead of "见解析".
- The fix is purely in the deterministic content generator; no DTO, view, or persistence change is required, and the math branch is unchanged.
- Cloud route already generates real problems (CloudIntelligenceService.similarProblems), so no change needed there.

Defense-in-depth (recommended, optional): in DrillModel.check() / DrillRunnerCard, detect a non-checkable sentinel answer (problem.answer is empty or equals "见解析") and switch that single question into a reveal-and-self-assess mode ("我会了 / 再想想") instead of auto-grading, so any future placeholder can never silently mark a real answer wrong or corrupt mastery. The content fix above is the primary, sufficient remedy; this guard is belt-and-suspenders.

(Reporting note: the catalog actually has 2 Chinese + 2 English + 1 Physics non-math KPs, not 4+4+1 as the original finding stated — but all 5 are selectable, so the defect and fix are unchanged.)

### [6] P1 Drill questions never count toward the daily contribution heatmap / streak
- area: Drill / Exam / Essay practice
- file: Features/Practice/Drill/DrillModel.swift @ persistResults(context:existingMasteries:) lines 346-398
- category: persistence | userRequested: True
- evidence: persistResults inserts a PracticeSession + PracticeAttempts and calls context.saveLogging() but NEVER calls ActivityRecorder.log(...). The contribution heatmap, streak, and 答题足迹 total are computed purely from ActivityLog.count via ContributionStats.questionsByDay (ActivityRecorder.swift:109-116, which only counts `where log.count > 0`). ActivityRecorder's doc-comment (lines 6-12) explicitly claims 练习/靶向练习 funnel through ActivityRecorder.log; the Drill does not, so completed targeted drills contribute 0 questions to the GitHub-style chart the user required.
- FIX: In DrillModel.persistResults, log the activity exactly once before the single saveLogging() so it is atomic with the session write and covered by the existing `saved` guard. Insert immediately after the attempts loop (after line 394, before `context.saveLogging()` on line 396):

    // Feed the contribution heatmap / streak / 答题足迹 — every learning surface
    // funnels question counts through ActivityRecorder so the chart counts drills too.
    let totalSeconds = outcomes.reduce(0) { $0 + $1.timeSpent }
    ActivityRecorder.log(
        context,
        kind: .drill,
        subject: target.subject,
        questions: outcomes.count,
        minutes: totalSeconds / 60,
        detail: "靶向练习 \(correctCount)/\(outcomes.count) · \(target.name)"
    )

This writes to the same ModelContext, so the existing context.saveLogging() persists the new ActivityLog alongside the session in one save (ActivityRecorder.log calls saveLogging internally too, which is harmless but redundant — acceptable). The `guard !saved` at line 347 plus `saved = true` at line 397 already guarantee the log is written exactly once per finished session, including across restart() which resets saved=false for a genuinely new run. minutes is Double (ActivityRecorder.log signature), and totalSeconds/60 yields Double minutes — correct. Recommend filing a sibling P1 to add the identical call to ExamModel.persistResults (kind: .exam) since it has the same gap.

### [7] P1 Drill results have no way to save wrong questions to the 错题本
- area: Drill / Exam / Essay practice
- file: Features/Practice/Drill/DrillRunnerViews.swift @ DrillResultsView.actionButtons (lines 324-336) and DrillModel (no addToMistakes equivalent)
- category: missing-feature | userRequested: True
- evidence: DrillResultsView offers only '再练一组' and '查看学习报告'. There is no '加入错题本' action and DrillModel has no MistakeItem-insertion method (grep for 加入错题本/addToMistake/MistakeItem in Features/Practice/Drill/ returns nothing). Sibling features do this: ArithmeticGradingModel.addWrongItemsToNotebook (ArithmeticGradingModel.swift:152) and ExamModel.addToMistakes (ExamModel.swift:468). So a learner who gets drill questions wrong has no path to bank them, breaking the required wrong-question → bank → AI-review-question loop for the 举一反三 surface.
- FIX: Add a banking method to DrillModel and surface it as a button in DrillResultsView, mirroring the sibling pattern.

1) In DrillModel (DrillModel.swift), add idempotency state and a method near persistResults:

  private(set) var savedMistakeProblemIDs: Set<String> = []
  var wrongOutcomes: [DrillOutcome] { outcomes.filter { !$0.isCorrect } }

  @discardableResult
  func addWrongOutcomesToNotebook(context: ModelContext) -> Int {
      let subject = selectedTarget?.subject ?? .math
      var added = 0
      for outcome in wrongOutcomes where !savedMistakeProblemIDs.contains(outcome.problemID) {
          let item = MistakeItem()
          item.subject = subject
          item.questionText = outcome.question
          item.studentAnswer = outcome.typedAnswer.isEmpty ? "未作答" : outcome.typedAnswer
          item.correctAnswer = outcome.correctAnswer
          item.errorReason = outcome.typedAnswer.isEmpty ? "练习中未作答，需巩固。" : "答案有误，需订正。"
          item.errorType = .concept
          item.mastery = .weak
          item.knowledgePointIDs = outcome.knowledgePointID.isEmpty ? [] : [outcome.knowledgePointID]
          // DrillOutcome carries no steps; recover them from the matching GeneratedProblem.
          item.steps = problems.first { $0.id == outcome.problemID }?.steps ?? []
          item.createdAt = .now
          item.nextReviewAt = .now
          context.insert(item)
          savedMistakeProblemIDs.insert(outcome.problemID)
          added += 1
      }
      if added > 0 { context.saveLogging(); HapticEngine.play(.success) }
      return added
  }
  var allWrongSaved: Bool { !wrongOutcomes.isEmpty && wrongOutcomes.allSatisfy { savedMistakeProblemIDs.contains($0.problemID) } }

Also reset savedMistakeProblemIDs in resetRunner()/restart() so 再练一组 starts clean.

2) In DrillResultsView (DrillRunnerViews.swift:236-337), add an onAddToNotebook closure property and a leading button in actionButtons (only when wrongOutcomes is non-empty), matching ArithmeticResultsView.swift:140-150:

  var onAddToNotebook: () -> Void
  ...
  if !model.wrongOutcomes.isEmpty {
      Button(action: onAddToNotebook) {
          Label(model.allWrongSaved ? "已加入错题本（\(model.savedMistakeProblemIDs.count) 题）" : "错题加入错题本",
                systemImage: model.allWrongSaved ? "checkmark.circle.fill" : "book.closed.fill")
      }
      .buttonStyle(.db(.primary, fullWidth: true))
      .disabled(model.allWrongSaved)
      .opacity(model.allWrongSaved ? 0.6 : 1)
  }

3) In DrillView.finishedPhase (DrillView.swift:272-284), pass the closure (context is already in scope there):

  onAddToNotebook: { model.addWrongOutcomesToNotebook(context: context) }

Update the #Preview for DrillResultsView (if present) to pass an empty onAddToNotebook: {} closure so it compiles. Keep 再练一组 as .primary or demote to .secondary as desired; the banking button takes visual priority like the Arithmetic screen.

### [8] P1 TTSService.isSpeaking never resets after narration completes → perpetual 'speaking' indicator in tutor blackboard & voice bar
- area: AI 伙伴 (chat) + Tutor
- file: Services/Speech/SpeechService.swift @ TTSService.speak(_:language:rate:) line 24-32; isSpeaking declared line 18; only reset in stop() line 34-37
- category: ux | userRequested: False
- evidence: speak() sets `isSpeaking = true` (line 31) but there is no AVSpeechSynthesizerDelegate and no `speechSynthesizer(_:didFinish:)` handler, so isSpeaking is only ever flipped back to false by an explicit stop() call. grep confirms the only `isSpeaking = false` is in stop(). TutorSessionView passes `tts.isSpeaking` into TutorBlackboard (line 163) and TutorVoiceBar (line 188); TutorBlackboard renders the animated ChalkWaveform whenever isSpeaking is true (TutorBlackboard.swift line 118-122) and TutorVoiceBar shows VoiceWaveform and hides the '点击展开字幕' hint while isSpeaking is true (TutorVoiceBar.swift line 40-48). After the first segment narrates, the synthesizer finishes but isSpeaking stays true, so both indicators animate forever (until the next narrate() which calls stop() then speak()=true again) — i.e. the board always looks like the teacher is talking, even during silent reading/checkpoints.
- FIX: Add an AVSpeechSynthesizerDelegate shim so isSpeaking tracks real narration. Keep TTSService @MainActor @Observable; do NOT make TTSService itself the delegate (that would force nonisolated callbacks and fight isolation).

In Services/Speech/SpeechService.swift:

1) Add a nonisolated delegate shim class at file scope:

    private final class TTSDelegate: NSObject, AVSpeechSynthesizerDelegate {
        // Called on an arbitrary thread; hop to the main actor.
        var onFinish: (@Sendable () -> Void)?
        nonisolated func speechSynthesizer(_ s: AVSpeechSynthesizer, didFinish u: AVSpeechUtterance) { onFinish?() }
        nonisolated func speechSynthesizer(_ s: AVSpeechSynthesizer, didCancel u: AVSpeechUtterance) { onFinish?() }
    }

2) In TTSService, hold the shim and wire it in init:

    private let ttsDelegate = TTSDelegate()
    public init() {
        synthesizer.delegate = ttsDelegate
        ttsDelegate.onFinish = { [weak self] in
            Task { @MainActor in self?.isSpeaking = false }
        }
    }

(Capture the closure as @Sendable; the hop via Task { @MainActor } satisfies Swift 6 isolation since isSpeaking is MainActor-isolated.)

3) Leave speak()/stop() as-is. speak() still sets isSpeaking = true after synthesizer.speak(utterance); the early-return guard (enabled/empty text) correctly leaves isSpeaking untouched because no utterance is enqueued. stop() still sets isSpeaking = false synchronously and, because it calls stopSpeaking(at:.immediate), the delegate's didCancel may also fire — the redundant set to false is harmless.

This makes the tutor ChalkWaveform/VoiceWaveform stop when narration actually ends, restores the correct "点击展开字幕" hint during silent reading/checkpoints, and fixes the stuck read-aloud toggles in RecognizeAnythingView, DocumentDetailView, ClassicalView, and TranslationView so users can re-read after the first playback finishes. No call sites need to change. Verify by building both iOS and macOS schemes (TTS is on-device on both); manually: open 豆包老师, let one segment narrate to a checkpoint and confirm the waveform stops; in RecognizeAnything, read a result aloud, let it finish, and confirm the button returns to "朗读讲解" and a second tap re-reads.

### [9] P1 Knowledge-graph node "去练习" opens an untargeted drill, dropping the node's knowledge point
- area: 豆包课堂 / 文档问答 / 知识图谱
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Knowledge/Graph/KnowledgeGraphView.swift @ NodeDetailSheet.actions, line 624-630 (the "去练习" Button)
- category: navigation | userRequested: True
- evidence: The node-detail sheet's drill button calls `perform { router.openTool(.drill, regular: regular) }`. `AppRouter.openTool` (AppRouter.swift:141-148) routes `.drill` through its `default` case to `navigate(.tool(.drill))`, which `AppDestinations.routeView`/`toolView` (AppDestinations.swift:84) resolves to `DrillView()` with NO target. The DrillModel only pre-selects a knowledge point when given a `preselected` id (DrillModel.swift:229-249). By contrast, the equivalent button in KnowledgePointView (KnowledgePointView.swift:502) correctly calls `router.openDrill(knowledgePointID: entity.id, ...)` → `.drill(knowledgePointID:)` → `DrillView(targetKnowledgePointID: kpID)`. So tapping 去练习 on a specific graph node lands on the generic drill picker (typically auto-selecting the weakest point), not the node the user tapped — an inconsistent, surprising result and a miss against the explicit targeted-drill requirement.
- FIX: In KnowledgeGraphView.swift NodeDetailSheet.actions (the 去练习 Button at lines 624-626), capture the node id before the closure and route through the existing targeted-drill path, mirroring the 讲一讲 button just above. Replace:

    Button {
        let regular = isRegular
        perform { router.openTool(.drill, regular: regular) }
    } label: { Label("去练习", systemImage: "pencil.and.outline") }

with:

    Button {
        let id = node.id
        let regular = isRegular
        perform { router.openDrill(knowledgePointID: id, regular: regular) }
    } label: { Label("去练习", systemImage: "pencil.and.outline") }

This reuses AppRouter.openDrill → Route.drill(knowledgePointID:) → DrillView(targetKnowledgePointID:) so the drill pre-selects the tapped node's knowledge point, matching KnowledgePointView's 去练习. No other changes needed; `regular`/`isRegular` already in scope.

### [10] P1 ActivityLog is created in only Focus and Exam; all other learning flows log nothing, so every activity chart undercounts
- area: Home / Today / Profile / Reports / Achievements / Focus
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Models/PlanAndActivity.swift @ ActivityLog @Model (lines 42-60); producers: FocusTimerView.swift:375-382 and ExamModel.swift:431-438 ONLY
- category: ai-wiring | userRequested: True
- evidence: grep for 'ActivityLog(' yields exactly 4 production-relevant sites: FocusTimerView (focus), ExamModel (practice), plus sample/preview seed data. WorkbookGradingModel, WorkbookResultView, DrillModel, DictationSessionModel, ArithmeticGradingModel, EssayGradingModel, SolveResultView, and QuestionBankModel each contain ZERO 'ActivityLog' references (verified per-file). Consequence: TodayView.minutesToday/weekBuckets, WeeklyActivityCard, AchievementsView.heatStrip, and ReportsView 学习时长/kindSlices/activeDayCount are all derived solely from ActivityLog, so a learner who grades a workbook, practices the question bank, solves photos, drills, does dictation/arithmetic/essay/vocabulary sees 0 minutes today, a flat empty week chart, an empty heat-strip, and a report that hits hasInsufficientData (ReportModels.swift:255). 'AI everywhere/fully integrated' is broken: the AI-driven core flows do not feed the dashboards.
- FIX: ActivityRecorder.log already exists and is the right funnel (it's wired into WorkbookGradingModel.swift:90). Add one ActivityRecorder.log call at each flow's confirmed-completion/persist point, recording elapsed minutes and question count with the correct ActivityKind:\n- SolveResultView/SolveModel: on successful solve persist → kind:.solve, questions:1, minutes: small elapsed.\n- DictationSessionModel.persistResult (:317): kind:.dictation, questions: graded word count, minutes: session elapsed.\n- EssayGradingModel.persist (:151): kind:.essay, questions:1, minutes: elapsed.\n- QuestionBankModel: on practice completion (when learner answers generated problems) → kind:.practice, questions: answered count.\n- DrillModel.persistResults (:382, inside the saved-once guard): kind:.drill, questions: outcomes.count, minutes: run elapsed.\n- ArithmeticGradingModel.grade/addWrongItemsToNotebook: kind:.practice (or add an .arithmetic kind), questions: total items graded, minutes: elapsed.\n- WordDeckReviewView.applyRating (:399-412): accumulate and on session end log kind:.vocabulary, questions: reviewedCount.\n- ExamModel.swift:432-438: replace the manual ActivityLog build with ActivityRecorder.log(kind:.exam, questions: outcomes.count, minutes: elapsed) so the heatmap also counts exam questions (currently exam contributes minutes only, count=0).\n- TutorSessionView: optional, log kind:.tutor minutes for time charts.\nEach call should be idempotent (guard behind the existing 'saved/persisted' flag) so re-grading/re-finishing doesn't double-count. After wiring, run a session in each flow and confirm TodayView minutesToday/weekBuckets, WeeklyActivityCard, ContributionCard heat strip, and ReportsView 学习时长/kindSlices/activeDayCount all populate.

### [11] P1 Streak (streakDays / StudyStreak) is never incremented by any user action — a frozen seeded number across 6 surfaces
- area: Home / Today / Profile / Reports / Achievements / Focus
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Models/LearnerProfile.swift @ LearnerProfile.streakDays (line 24); StudyStreak in PlanAndActivity.swift:62-70; only writers are SampleData.swift:40,46 and ProfilePreviewData.swift:34
- category: persistence | userRequested: True
- evidence: grep for 'streakDays =' / 'streakDays +=' and StudyStreak '.current=/.lastCheckIn=' finds writes ONLY in SampleData and preview data (streakDays=7, streak.current=7). No check-in / daily-rollover logic exists anywhere (grep for 打卡/checkIn finds only display strings). Meanwhile streak is shown on Home (DBStreakView line 132, encouragement line 585), Today (header line 331, footer 605), Profile header (ProfileHeaderCard), and Achievements (statStrip line 239 + streakSection + 4 streak badges streak_3/7/30/100). The displayed '已连续学习 7 天' never changes with use and streak badges beyond the seed can never unlock.
- FIX: Stop persisting/seeding a stale streak counter; derive the live streak from ActivityLog dates using the already-correct ContributionStats.currentStreak(logs), the same source already used by ContributionCard.

Minimal, concrete changes:
1) Add `@Query(sort: \ActivityLog.date) private var logs: [ActivityLog]` to the views that show the streak but don't already have it: HomeView, TodayView, ProfileView/ProfileHeaderCard. (AchievementsView already has `logs`.)
2) Replace every read of `profile?.streakDays` used for streak display with `ContributionStats.currentStreak(logs)`:
   - HomeView.swift:132 (DBStreakView) and the streak read at 585.
   - TodayView.swift:332 (DBStreakView), :371/:375 (accessibility), :605/:610 (footer).
   - ProfileHeaderCard.swift:40 (pass a `streakDays` param computed from logs in ProfileView) and ProfileView.swift:103 (ProfileStatsCard streakDays).
   - AchievementsView.swift: change the `streakDays` computed property (lines 55-58) to `ContributionStats.currentStreak(logs)` so statStrip (239), streakSection (267), AchievementMetrics (62), and the streak_3/7/30/100 badges all advance correctly.
3) Remove the now-dead seeded writes so nothing masks the derivation: delete `profile.streakDays = 7` (SampleData.swift:40) and the StudyStreak seed (SampleData.swift:45-46); optionally drop `LearnerProfile.streakDays` and the `StudyStreak` model entirely if no longer referenced (keep them if CloudKit migration risk, but stop reading them for display). Keep ProfilePreviewData as-is for SwiftUI previews only.

This requires no new persistence or rollover code (currentStreak already handles same-day no-op, yesterday-hold, and gap-reset). Verify: log a question today → streak shows 1 across Home/Today/Profile/Achievements; log again tomorrow → 2; skip a day → resets; verify streak badges unlock as the count crosses 3/7/30. Alternative (more work, not recommended): implement explicit StudyStreak.lastCheckIn rollover inside ActivityRecorder.log and mirror to streakDays — but that duplicates logic that ContributionStats already provides and risks drift, so prefer the derive-from-logs approach.

### [12] P1 Profile 历史记录 / 收藏 / 错题本 all route to the same 错题本 destination
- area: Home / Today / Profile / Reports / Achievements / Focus
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Profile/ProfileView.swift @ ProfileNavigationGroup wiring, lines 116-119 (onHistory/onFavorites/onMistakes all = openMistakes())
- category: navigation | userRequested: True
- evidence: onHistory:{openMistakes()}, onFavorites:{openMistakes()}, onMistakes:{openMistakes()} — three distinct labeled rows ('历史记录' subtitle '拍题与答疑的解题记录', '收藏' subtitle '标记的好题与讲解', '错题本' subtitle '自动收录，按知识点复习') all open the mistakes notebook. ProblemRecord history and saved/favorited items exist as data (ProfileView already @Query's problems and computes savedCount) but are unreachable — the labeled history and favorites entries are effectively non-functional.
- FIX: Give 历史记录 and 收藏 their own real destinations instead of reusing openMistakes(). Minimal, precise plan:

1) Add two push routes in /Users/rogerlin/Downloads/豆包爱学/豆包爱学/App/AppRouter.swift Route enum: `case solveHistory` and `case favorites` (or `case savedItems`). 

2) Register their detail/push destinations in AppDestinations (the switch that maps Route -> view, used by AppShell.sectionRoot and the navigationDestination(for: Route.self) builders). 

3) Create two thin SwiftData-backed list views under Features/Profile (mirroring existing card/list idioms and adaptive iPhone/iPad/Mac layout, like MistakeNotebookView): 
   - SolveHistoryView: @Query(sort: \ProblemRecord.createdAt, order: .reverse) problems: [ProblemRecord]; each row shows recognizedText/subject/createdAt and taps through to re-open the solution (reuse SolveResultView or a read-only detail). Empty state mirrors existing DBStateView usage. 
   - FavoritesView: @Query the favorited set. Note: the row subtitle is "标记的好题与讲解" and favoriteCount currently mixes ProblemRecord.savedToMistakes + MistakeItem — decide on ONE source of truth. Cleanest is to show ProblemRecord where savedToMistakes==true (the user's explicitly saved good problems) and exclude the auto-收录 mistakes so 收藏 and 错题本 are distinct; alternatively reuse the BankedQuestion "收藏到题库" set if that is the intended favorites surface. Whichever is chosen, make favoriteCount in ProfileView match that exact predicate so the badge is truthful.

4) Re-wire ProfileView.swift loadedContent: onHistory: { router.navigate(.solveHistory, regular: isRegular) } ; onFavorites: { router.navigate(.favorites, regular: isRegular) } ; keep onMistakes: { openMistakes() }.

This keeps the change localized to AppRouter (2 enum cases + 2 destinations), 2 small new list views, and a 2-line re-wire in ProfileView, with no churn to MistakeNotebook. Verify on both idioms: iPhone push and iPad/Mac detail-column navigation.

### [13] P1 detailPath is never reset when the sidebar section changes (stale pushed views on iPad/Mac)
- area: iOS / iPadOS / macOS parity (cross-cut)
- file: App/AppShell.swift @ splitView, lines 82-87 (detail NavigationStack bound to $router.detailPath with root sectionRoot(router.sidebarSelection)); AppRouter has no detailPath reset on sidebarSelection change (App/AppRouter.swift:110-138)
- category: navigation | userRequested: True
- evidence: The detail column is `NavigationStack(path: $router.detailPath){ sectionRoot(sidebarSelection ?? .home) ... }`. Pushing a Route (e.g. Home → router.navigate(.course(id), regular: true) at HomeView.swift:353, or router.openTool(...) at HomeView.swift:389) appends to detailPath. Switching the sidebar (List selection at AppShell.swift:68, menu '前往'/'问豆包' at StudyCommands.swift:33,44, Home '全部' at HomeView.swift:373, Profile.openMistakes at ProfileView.swift:174-175) changes the stack ROOT but leaves detailPath populated. grep confirms NOTHING clears detailPath on a sidebar change (only popToRoot, never called from these paths). Result on iPad/macOS: user selects '知识图谱' in the sidebar but still sees a previously-pushed course/mistake detail layered on top of the new root — the new section appears not to open. This is the classic SwiftUI footgun where the path must be reset when the stack's root identity changes.
- FIX: Reset the detail stack at the single chokepoint — the sidebarSelection stored property — rather than scattering .onChange handlers. In App/AppRouter.swift, change line 110 to add a didSet:

public var sidebarSelection: AppSection? = .home {
    didSet {
        if oldValue != sidebarSelection { detailPath = NavigationPath() }
    }
}

Rationale: the List(selection:) binding (AppShell.swift:68), both macOS menus (StudyCommands.swift:33,44), HomeView.swift:373, ProfileView.swift:177, and the deep-link handle() cases all write this one property, so the didSet covers every entry point with no AppShell .onChange needed (the prior auditor's secondary .onChange is redundant). The oldValue != sidebarSelection guard prevents clobbering the stack on idempotent re-selection of the same section. The deep-link cases in handle() (AppRouter.swift:165-178) already set sidebarSelection BEFORE pushing (lines 167/171 for dictation, 174 for mistakes), so their ordering is correct and survives this change: setting the section first clears the path via didSet, then the subsequent push lands on the fresh stack. Verify on both iPad and Mac: push a detail (Home → course card), switch sections via sidebar tap and via ⌘-number 前往 menu, and confirm the new section root shows with an empty detail stack each time.

### [14] P1 Question-counting GitHub-style contribution chart is absent; activity charts count minutes only and are fed by just 2 of ~15 learning features
- area: Stubs / placeholders / dead code / persistence gaps (cross-cut)
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Models/PlanAndActivity.swift @ ActivityLog (lines 42-60); writers: only FocusTimerView.swift:376 and ExamModel.swift:432. Consumers: WeeklyActivityCard.swift:30-35, AchievementsView.swift:324-345 (heatStrip), TodayView.swift:42, ReportsView.swift:25
- category: missing-feature | userRequested: True
- evidence: The user explicitly required 'questions counted in a GitHub-style daily contribution chart.' In committed HEAD, no such chart exists: WeeklyActivityCard aggregates ActivityLog.minutes into a 7-day bar chart and AchievementsView.heatStrip buckets minutes into a 14-day strip (minutesByDay[day] += log.minutes) — neither is a multi-week GitHub grid and neither counts questions. Worse, the underlying data is nearly empty in real use: grep for `ActivityLog()` shows the only feature writers are FocusTimerView (kindRaw="focus") and ExamModel (kindRaw="practice"). SolveResultView.persist() writes a ProblemRecord but no ActivityLog; WorkbookGradingModel.persist() writes a WorkbookGradeRecord but no ActivityLog; DrillModel writes a PracticeSession (kindRaw="targeted") but no ActivityLog; DictationSessionModel, EssayGradingModel, ArithmeticGradingModel, TutorSessionView and the vocabulary flow likewise persist their own records but never log an ActivityLog. So solving/grading dozens of questions contributes nothing to the streak heat-strip or weekly chart.
- FIX: Do NOT re-create the chart or infrastructure — both already exist (ContributionCard, DBContributionGraph, ActivityRecorder, ContributionStats, ActivityLog.count). The only work is wiring the remaining question-producing flows to ActivityRecorder.log so each contributes its question count. Add ONE call per flow at the point a graded/solved result is finalized:

1. ExamModel.swift:432-438 — the existing ActivityLog never sets count. Either set `log.count = outcomes.count` on the existing entry, or replace lines 432-438 with `ActivityRecorder.log(context, kind: .exam, subject: selectedSubject, questions: outcomes.count, minutes: max(1, (elapsed/60).rounded()), detail: "模拟测验 \(correctCount)/\(outcomes.count)")`. (outcomes.count is in scope here.)
2. SolveResultView.swift persist(_:context:) (~line 803) — after the ProblemRecord write, add `ActivityRecorder.log(context, kind: .solve, subject: solved.subject, questions: 1, detail: "拍题搜题")`.
3. WorkbookGradingModel.swift:90 — already correct (questions: result.total). No change.
4. DrillModel.swift persistResults (line 346) — add `ActivityRecorder.log(context, kind: .drill, subject: <target.subject>, questions: outcomes.count, detail: "靶向练习")` after the session/attempts insert.
5. DictationSessionModel.swift — in the result-persist path, add `ActivityRecorder.log(context, kind: .dictation, questions: grading.total, detail: "听写")` (grading.total is available, see line 321).
6. ArithmeticGradingModel.swift — after persist, add `ActivityRecorder.log(context, kind: .practice, subject: .math, questions: graded.total, detail: "口算批改")` (graded.total available, see line 60).
7. EssayGradingModel.swift persist (line 151) — add `ActivityRecorder.log(context, kind: .essay, subject: subject, questions: 1, detail: "作文批改")`.
8. TutorSessionModel/TutorSessionView — on a resolved tutoring problem, add `ActivityRecorder.log(context, kind: .tutor, questions: 1, detail: "AI 辅导")`.
9. Vocabulary review (WordDeckReviewView.swift) — on review completion, add `ActivityRecorder.log(context, kind: .vocabulary, questions: <reviewedCount>, detail: "背单词")`.

Keep each call idempotent with the existing persist guards (e.g. ExamModel's `saved` flag, savedRecordID in Solve/Workbook) so re-grading the same capture doesn't double-count. FocusTimerView.swift:376 should stay count-unset (time-only, correct). After wiring, grep `ActivityRecorder.log` should show one caller per learning surface (not just Workbook), and a quick manual run of each flow should light a cell on ContributionCard in 个人中心/今日/报告.

### [15] P1 Tools hub (工具 tab) is still the WAVE-0 placeholder: monotone tinting, hardcoded spacing, no section affordances, no search — visibly less finished than the rest of the app
- area: Visual design, consistency & UX polish (cross-cut)
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Tools/ToolsHubView.swift @ Lines 5 (header comment 'WAVE-0 PLACEHOLDER'), 23-31 (grid + tile construction)
- category: ux | userRequested: True
- evidence: The file still declares itself a placeholder: '//  WAVE-0 PLACEHOLDER (replaced by the Tools feature agent in Wave 1).' Every tool tile is tinted identically with `tint: .dbPrimary` (line 27), producing a flat wall of orange icons across all 23 tools, whereas HomeView gives each tool a category color via `toolTint(for:)` (HomeView.swift:385/399: qa→dbPrimary, grade→dbSecondary, memory→dbAccent, expression→dbInfo, extend→dbSuccess). The grid also bypasses the spacing token with raw literals: `LazyVGrid(columns: Array(repeating: GridItem(spacing: 12), count: isRegular ? 5 : 4), spacing: 12)` (line 24). Section headers are bare `DBSectionHeader(category.displayName)` with no systemImage or subtitle (line 23), unlike Home/Today/Profile which always pass an icon. There is no DBSearchField even though DBSearchField exists and SearchView is reachable only from Home's toolbar — the dedicated Tools tab, the natural place to find a tool, has no in-page search.
- FIX: Promote ToolsHubView from placeholder to a finished hub. Centralize the metadata on ToolCategory so Home and ToolsHub share one source of truth instead of duplicating Home's private toolTint(for:).

1) In Models/AppEnums.swift, extend `ToolCategory` with shared presentation metadata (import SwiftUI in that file or put the Color extension in a UI-layer file if you keep AppEnums framework-free):
   - `var tileTint: Color` returning the same mapping as HomeView (qa→.dbPrimary, grade→.dbSecondary, memory→.dbAccent, expression→.dbInfo, extend→.dbSuccess).
   - `var symbolName: String` (e.g. qa→"questionmark.bubble.fill", grade→"checkmark.seal.fill", memory→"brain.head.profile", expression→"text.bubble.fill", extend→"sparkles").
   - `var subtitle: String` (a short Chinese descriptor per category).
   Add `var tileTint: Color { category.tileTint }` on ToolKind for ergonomics.

2) In HomeView.swift, replace the private `toolTint(for:)` (lines 399-407) and its call site (line 385 `tint: toolTint(for: tool)`) with `tint: tool.tileTint` so both screens stay in sync and the duplicated switch is removed.

3) In ToolsHubView.swift:
   - Delete the WAVE-0 placeholder comment (line 5).
   - Line 23: `DBSectionHeader(category.displayName, subtitle: category.subtitle, systemImage: category.symbolName)`.
   - Line 24: `LazyVGrid(columns: Array(repeating: GridItem(.flexible(), spacing: DBSpacing.md), count: isRegular ? 5 : 4), spacing: DBSpacing.md)`.
   - Line 27: `tint: tool.tileTint` instead of `.dbPrimary`.
   - Add a top `DBSearchField` bound to a `@State private var query = ""` and filter `ToolKind.allCases` by `displayName.localizedCaseInsensitiveContains(query)` (hide empty categories when filtering). Keep it simple — DBSearchField already exists; no need to wire `.searchable`/SearchView. This gives the dedicated Tools tab the in-page search it lacks on both iOS and macOS.

Verify on both the iPhone tab root and the iPad/Mac sidebar (AppDestinations.swift:22 and :39) since ToolsHubView is the root for both.

### [16] P2 AI 伙伴 (chat companion) is a top-level destination on iPad/Mac but only a buried tile on iPhone — IA parity gap for the app's core AI surface
- area: Navigation & Information Architecture (cross-cut)
- file: /Users/rogerlin/Downloads/豆包爱学/豆包爱学/App/AppRouter.swift @ AppTab enum (lines 13-32) vs AppSection enum (line 37); CompanionView reached via AppSection.companion (AppDestinations.swift:38) and ToolKind.knowledgeQA (AppDestinations.swift:78)
- category: navigation | userRequested: True
- evidence: On iPad/Mac the sidebar (AppSection) has a dedicated `companion` section ('AI 伙伴', bubble icon) as one of 9 top-level entries, and StudyCommands gives it ⌘L ('问豆包'). On iPhone the AppTab set is only `home, study, tools, me` — there is no chat/companion tab. The AI companion is therefore reachable on iPhone ONLY via Home's '问豆包' button (HomeView.swift:233) and the 工具 hub's '知识专家' tile (ToolKind.knowledgeQA). For an AI learning app whose central promise is the AI 搭子/chat, making it a primary top-level destination on one platform and a secondary tile on the other is an intuitiveness/parity inconsistency: an iPhone user has no persistent, always-one-tap path to the AI assistant.
- FIX: Give iPhone a persistent, always-reachable path to the AI companion to match the iPad/Mac sidebar's .companion section. Minimal options: (a) Add an aiChat case to AppTab (e.g. order home, study, aiChat, tools, me) with displayName "AI 伙伴" / symbol "bubble.left.and.bubble.right.fill", and map it in AppDestinations.tabRoot to CompanionView() — a near-exact parity match to AppSection.companion (one enum case + one switch arm). Or (b) preferred for iPhone idiom: add a persistent companion affordance available from every tab root (e.g. a small toolbar/principal button or floating action button in AppShell.tabView that calls router.openTool(.knowledgeQA, regular: false)), so chat is one tap from Study/Tools/Me, not just Home. Either restores iOS<->macOS IA parity for the core AI surface without disturbing existing routing. If choosing (a), note the macOS ⌘L already targets sidebarSelection=.companion, so no Mac change is needed; verify the new tab doesn't crowd the iPhone tab bar past comfortable density (5 is acceptable).

### [17] P3 Exam ActivityLog is written but its question count is never set, so exams also contribute 0 to the heatmap
- area: Drill / Exam / Essay practice
- file: Features/Exam/ExamModel.swift @ persistResults(...) lines 430-438 (the ActivityLog block)
- category: persistence | userRequested: True
- evidence: persistResults builds an ActivityLog with kindRaw="practice", detail and minutes set, but never sets `log.count`. ActivityLog.count defaults to 0 (Models/PlanAndActivity.swift:51). ContributionStats.questionsByDay ignores any log with count==0 (ActivityRecorder.swift:111 `where log.count > 0`), so even if the exam feature were reachable, its questions would not appear in the daily contribution chart, streak, or total. It also bypasses ActivityRecorder (hand-rolls the log) and uses kindRaw 'practice' instead of the canonical .exam kind.
- FIX: In Features/Exam/ExamModel.swift, replace the manual ActivityLog block (lines 432-438) with a single call to the canonical recorder so the count is set, the kind/icon are correct, and activity funnels through one place. Preserve the existing minutes computation rather than introducing a different formula:

    let elapsed = Date.now.timeIntervalSince(startedAt)
    ActivityRecorder.log(
        context,
        kind: .exam,
        subject: selectedSubject,
        questions: outcomes.count,
        minutes: max(1, (elapsed / 60).rounded()),
        detail: "模拟测验 \(correctCount)/\(outcomes.count)"
    )

This sets ActivityLog.count = outcomes.count (so exams appear in the heatmap/streak/total), uses kindRaw "exam" with the checklist icon, and reuses the recorder's saveLogging path. Because ActivityRecorder.log inserts AND saves, drop the separate context.insert(log) line; the surrounding session/attempt inserts remain saved by the model's own save. Caveat for the implementer: this is dead code today (ExamModel has no view/caller), so fixing it only pays off if the Exam feature is actually wired into navigation; otherwise prefer either deleting ExamModel.swift or shipping an ExamView. The higher-priority sibling fix is to add the same ActivityRecorder.log(... kind: .drill ...) call to DrillModel.persistResults, which IS reachable and currently logs no ActivityLog at all.

## LOW P2/P3 (30)

- [P2] (platform) PDF is accepted by the file importer but cannot actually be graded (OCR can't decode PDF; PDF bytes get mislabeled as JPEG to vision APIs) — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Workbook/WorkbookGradingView.swift @ .fileImporter allowedContentTypes lines 76-79; handleFileImport lines 283-289; Data.workbookImage(from:) in WorkbookImageInput.swift lines 102-109; OCRService.makeCGImage OCRService.swift lines 60-63; CloudChatClient.mimeType lines 233-242
    fix: Either (a) remove `.pdf` from allowedContentTypes so only true images are offered (simplest/honest), or (b) actually support PDF: in Data.workbookImage(from:), detect PDF (UTType from the URL or %PDF 
- [P2] (ai-wiring) Chinese vision-capable providers (Doubao/Qwen/GLM) are flagged supportsVision=false, so the app's namesake provider falls back to OCR-text and can't read the page image directly — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Services/Intelligence/Cloud/AIProvider.swift @ doubao lines 74-86, qwen 88-100, glm 102-114 (no supportsVision flag); vs anthropic/openai/gemini lines 170/185/200 which set supportsVision: true; consumed at CloudIntelligenceService.swift line 213
    fix: Add a vision-capable model and set supportsVision: true for the VL-capable Chinese providers (e.g. add doubao-vision-pro-32k / qwen-vl-max / glm-4v to their model lists and mark supportsVision: true).
- [P2] (ai-wiring) Re-banked generated problems drop knowledge points and explanation, degrading the re-banking loop — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/QuestionBank/QuestionBankModel.swift @ bankGenerated lines 84-100
    fix: In bankGenerated, when problem.knowledgePointID is non-empty set item.setKnowledgePoints([...]) by resolving the id back to a KnowledgeRef (the seed subject's knowledge points are available from the r
- [P2] (platform) macOS capture uses a plain open panel while the better Continuity-Camera helper is dead code (parity gap) — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Solve/CaptureSolveView.swift @ importFileMac(ocr:) lines 508-520; dead helper SolveContinuityCamera in SolveScanSupport.swift lines 145-167
    fix: Wire the macOS 导入图片 tile to SolveContinuityCamera.importImage() (returning Data) instead of the local importFileMac, then run that data through the existing recognizeMac/OCR path; relabel the tile to 
- [P2] (missing-feature) 举一反三 generates AI review questions but they cannot be re-banked from the Solve flow — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Solve/SolveResultView.swift @ SimilarProblemCard lines 609-671 (only a 先做做看 reveal button); no save action in SimilarProblemsSheet lines 548-607
    fix: Add a per-card "加入题库" button to SimilarProblemCard that maps the GeneratedProblem into a BankedQuestion (questionText=problem.question, correctAnswer=problem.answer, steps=problem.steps, subject=solve
- [P2] (ux) Answer-checker token split lets a bare variable name false-positive on algebra answers — Features/Practice/Drill/DrillModel.swift @ DrillAnswerChecker.isCorrect, token fallback lines 77-78 (identical logic in ExamAnswerChecker, ExamModel.swift:95-96)
    fix: When splitting on relational/operator characters, only accept a token match if the token is the right-hand-side value, not a lone identifier: drop tokens that are purely alphabetic single variables, e
- [P3] (design) Drill solution step numbers are off-by-one (start at 2) — Features/Practice/Drill/DrillRunnerViews.swift @ DrillStepRow body, line 206: Text("\(step.index + 1)")
    fix: Change DrillRunnerViews.swift:206 to `Text("\(step.index)")` to match every other step renderer, and update the #Preview's SolutionStep indices to 1-based (1,2,3) so the preview reflects production da
- [P2] (ux) Dead auto-title code: new 新对话 threads never get renamed from the user's first question — Features/Companion/ConversationView.swift @ send(to:) line 134: `if conversation.title == "新对话" { conversation.title = String(text.prefix(16)) }`
    fix: Rename on the first user turn regardless of the placeholder title. Replace the equality check with: `if conversation.sortedMessages.filter({ $0.role == .user }).count == 1 { conversation.title = Strin
- [P2] (stub) Speech recognition is fully simulated on both platforms — mic always returns a canned transcript — Services/Speech/SpeechService.swift @ SpeechRecognitionCoordinator.stopListening(simulated:) line 59-63; isAvailable returns false line 65
    fix: Wire SFSpeechRecognizer (iOS+macOS) / SpeechAnalyzer behind the existing coordinator: request authorization, run a live recognition task on captured audio, publish partial+final transcripts, and set i
- [P3] (ai-wiring) Companion → tutor suggestion handoff drops the conversation's subject/grade context — Features/Companion/ConversationView.swift @ dispatch(_:) line 177-185, case "tutor": router.present(.tutor(problemText: block.content, subject: .general, grade: .g5))
    fix: Pass available context: `router.present(.tutor(problemText: block.content, subject: conversation.subject ?? .general, grade: <profile grade if available, else .g5>))`. The conversation has no grade fi
- [P3] (design) ChatMessageEntity.route getter guards on the wrong field (roleRaw instead of routeRaw) — Models/ConversationModels.swift @ ChatMessageEntity.route getter line 56-59: `get { roleRaw.isEmpty ? nil : routeRaw.flatMap(IntelligenceRoute.init(rawValue:)) }`
    fix: Simplify to `get { routeRaw.flatMap(IntelligenceRoute.init(rawValue:)) }`. The flatMap already yields nil when routeRaw is nil, so no guard is needed.
- [P2] (persistence) Document summaries lose their AI provenance: reloaded cloud summaries show a Mock/local badge — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Courses/DocumentQA/DocumentDetailView.swift @ DocumentDetailModel.loadSummary, lines 69-77 (route: .mock hardcode); backed by DocumentEntity in Models/CourseAndDocument.swift:58-66
    fix: Add `public var summaryRouteRaw: String = IntelligenceRoute.mock.rawValue` (plus a computed `route` accessor) to DocumentEntity. In both DocumentQAView.summarize and DocumentDetailModel.loadSummary, p
- [P2] (ai-wiring) Cloud document-QA never returns citations, so the "引用原文" UI is dead on the real-AI route — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Services/Intelligence/Cloud/CloudIntelligenceService.swift @ answerAboutDocument, lines 419-432 (returns citedSpans: [])
    fix: Make answerAboutDocument request JSON like `{"answer":"...","citedSpans":["原文片段"]}` via the existing `json(_:system:user:)` helper (mirroring summarizeDocument's SummaryWire pattern), decode both fiel
- [P3] (ux) Knowledge-graph node tap hit-testing may be inaccurate when zoomed/panned — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Knowledge/Graph/KnowledgeGraphView.swift @ KnowledgeConceptMap.body / tapGesture, lines 291-295 and 373-392
    fix: Convert the tap location back through the current transform before hit-testing: `let local = CGPoint(x: (value.location.x - offset.width)/scale + (1-1/scale)*size.width/2 ... )` — simpler and more rob
- [P2] (persistence) Home '已解题' uses static profile.problemsSolved while Profile/Achievements use live problems.count — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Home/HomeView.swift @ statStrip DBValueStat value: "\(profile?.problemsSolved ?? 0)" (line 142)
    fix: Make Home's '已解题' use the live count: @Query ProblemRecord and show max(problems.count, profile.problemsSolved), matching ProfileView/Achievements. Alternatively increment profile.problemsSolved on ea
- [P2] (ai-wiring) Reports kind breakdown / time charts can only ever show focus + exam categories — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Reports/ReportBuilder.swift @ buildKindSlices (lines 112-122) consuming ActivityLog.kindRaw; ReportsView kindLegend (lines 246-256)
    fix: Resolved by wiring ActivityRecorder/ActivityLog into all learning flows (see P0). Ensure each flow passes a distinct kindRaw (workbook/solve/dictation/vocabulary/essay/drill/tutor/course) so the kind 
- [P2] (ai-wiring) RoutePolicy per-task cloud routing is entirely dead code and misleadingly fakes a cloud seam — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Services/Intelligence/RoutePolicy.swift @ enum IntelligenceTask (L12-23), RoutePolicy.resolve(_:) (L39-43), RoutePolicy.provider(for:) (L46-52), prefersCloudWhenAllowed (L17-22)
    fix: Delete the dead instance API: remove `enum IntelligenceTask`, the `preferred`/`onDeviceAvailable`/`cloudAvailable` stored properties, the initializer, `resolve(_:)`, and `provider(for:)`. Keep only `s
- [P2] (ai-wiring) 识万物 (RecognizeAnything) never uses cloud vision even on vision-capable providers; it discards the image and only sends the recognized label as text — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Tools/RecognizeAnything/RecognizeAnythingModel.swift @ enrichAndPublish(_:intelligence:grade:) L57-85, specifically the explainKnowledgePoint call at L70-74
    fix: Add an optional `imageData: Data?` to `ExplainRequest` (mirroring SolveRequest), and in CloudIntelligenceService.explainKnowledgePoint use `completeVision` when `imageData != nil && client.provider.su
- [P3] (ai-wiring) Cloud route badge claims .cloud for deterministic graders that never call the cloud (口算/听写/发音), overstating which results were AI-generated — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Services/Intelligence/Cloud/CloudIntelligenceService.swift @ gradeArithmetic L472-474, gradeDictation L475-477, scorePronunciation L478-480 (each: `var r = try await fallback.X(request); r.route = .cloud; return r`)
    fix: Stamp these results with the honest route of the engine that actually produced them — i.e. leave the offline result's `.mock`/`.onDevice` route (do not overwrite to `.cloud`). If a unified badge is de
- [P2] (navigation) 工具 hub category grouping is semantically wrong for 今日/专注/成就 (placed in 记忆) and 举一反三 (placed in 批改) — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Models/AppEnums.swift @ ToolKind.category computed property, lines 322-334
    fix: Add a dedicated ToolCategory (e.g. `.plan` '规划/学习习惯' for 今日/专注/成就, or fold 成就 into a 'progress' group with 学习报告) and move `.drill` into a practice group (e.g. `.memory` 记忆/练习 or a new `.practice`). Ad
- [P2] (navigation) KnowledgeExplanationView is a complete, AI-wired reusable card with NO embed site (orphan component) — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Knowledge/Point/KnowledgeExplanationView.swift @ struct KnowledgeExplanationView (line 17); only references are its own #Preview at lines 156 and 162
    fix: Embed KnowledgeExplanationView where its design intends — strongest fit is inside MistakeDetailView (a compact '讲讲这个知识点' card under the question) and/or HomeView's 今日靶向练习 expanded state, passing onExt
- [P3] (stub) Dead navigation leftovers: ComingSoonView and Wave0Welcome are defined but never instantiated; ToolsHubView carries a misleading 'WAVE-0 PLACEHOLDER' header — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/App/ComingSoonView.swift @ ComingSoonView (ComingSoonView.swift:11, zero usages); Wave0Welcome (RootView.swift:26, zero usages); stale comment ToolsHubView.swift:5
    fix: Delete App/ComingSoonView.swift and the private Wave0Welcome struct in RootView.swift (both are unreferenced dead code; removing them is zero-risk). Update the ToolsHubView.swift header comment to des
- [P2] (navigation) Three of the four '我的内容' rows (历史记录, 收藏, 错题本) all navigate to the same MistakeNotebook destination — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Profile/ProfileView.swift @ loadedContent → ProfileNavigationGroup callbacks, lines 116-119
    fix: Either (a) build/route real 历史记录 (ProblemRecord history) and 收藏 (savedToMistakes/favorited items) destinations and wire onHistory/onFavorites to them, or (b) if those screens are intentionally deferre
- [P2] (platform) No macOS Settings/Preferences scene or ⌘, command — core AI-model settings only reachable by drilling into Profile — App/DoubaoAiXueApp.swift @ Scene body lines 22-38 (WindowGroup + #if os(macOS) .commands{StudyCommands}); no `Settings { }` scene; AISettingsView reached only via ProfileSettingsCard.swift:266-268 NavigationLink
    fix: Add a macOS `Settings { AISettingsView().environment(aiStore).environment(\.intelligence, ...) }` scene in the App body under #if os(macOS) (gives ⌘, + an App-menu 设置… for free), or add a `CommandGrou
- [P2] (platform) macOS menu omits first-class flows (听写 / 错题本 / 作文批改 / 实时扫题) that are exposed as Siri intents on iOS — App/StudyCommands.swift @ 学习 CommandMenu lines 20-40; compare with Intents/StudyAppIntents.swift titles (听写:78, 复习错题:99, 找豆包老师:119)
    fix: Add menu items in StudyCommands 学习 menu: `Button("听写"){ router.openTool(.dictation, regular:true) }`, `Button("错题本"){ router.sidebarSelection = .mistakes }`, `Button("作文批改"){ router.openTool(.gradeEss
- [P3] (platform) Badge detail sheet relies only on .presentationDetents (ignored on macOS) — no macOS frame, unlike the 智能出题 sheet — Features/Achievements/AchievementsView.swift @ badgeDetailSheet, lines 463-550 (NavigationStack...); `.presentationDetents([.medium, .large])` at line 549; no `#if os(macOS) .frame(minWidth:minHeight:)`
    fix: Mirror the QuestionBankView pattern: add `#if os(macOS) .frame(minWidth: 420, minHeight: 560) #endif` to the badgeDetailSheet's root, keeping the existing iOS presentationDetents. Audit other detent-o
- [P3] (stub) ComingSoonView is dead code (no references anywhere) — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/App/ComingSoonView.swift @ struct ComingSoonView (lines 11-30)
    fix: Delete App/ComingSoonView.swift. It is unused; removing it eliminates the only 'coming soon / 即将上线' placeholder in the app and removes confusion about whether any destination is unfinished.
- [P3] (stub) Dead placeholder view ComingSoonView is unreferenced — leftover scaffolding — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/App/ComingSoonView.swift @ Whole file (struct ComingSoonView, lines 11-30)
    fix: Delete App/ComingSoonView.swift (and its #Preview). Every AppDestination already resolves to a real feature view, so removing it cleans up the 'coming soon' implication with no behavioral change. If a
- [P2] (ux) Chat composer mic/send buttons lack accessibility labels — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Companion/ConversationView.swift @ composer(_:), lines 84-110 (mic Button) and (send Button)
    fix: Add `.accessibilityLabel(speech.isListening ? "停止语音输入" : "语音输入")` to the mic button and `.accessibilityLabel("发送")` to the send button. Apply the same to the equivalent mic/send controls in TutorSessi
- [P3] (design) Inconsistent foreground-color API: lone deprecated .foregroundColor in EssayFeedbackView — /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Practice/Essay/EssayFeedbackView.swift @ Line 329: `return Text(feedback.polishedText).foregroundColor(.dbTextPrimary)`
    fix: Change `.foregroundColor(.dbTextPrimary)` to `.foregroundStyle(Color.dbTextPrimary)` to match the rest of the app's foreground-styling convention.

## REFUTED (3)

- Workbook grading never records activity — graded questions are invisible to the daily contribution chart, streak, reports, and achievements — Refuted as a false positive. I read the actual cited file, /Users/rogerlin/Downloads/豆包爱学/豆包爱学/Features/Workbook/WorkbookGradingModel.swift. The finding's central claim — that grade(using:ocr:context:
- No GitHub-style daily contribution heatmap; question counts are not charted anywhere — REFUTED. The finding's central claim — "No GitHub-style daily contribution heatmap; question counts are not charted anywhere" and "ContributionStats.dailySeries has ZERO call sites and no view uses it
- Contribution-chart scaffolding (ActivityRecorder / ContributionCard / DBContributionGraph) exists but has zero callers and is rendered by no view — The finding's core, severity-defining claims are factually false against the actual code.

1. "grep -rn ActivityRecorder outside its own file returns nothing — never called": FALSE. /Users/rogerlin/Do