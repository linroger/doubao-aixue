==================================================================
 豆包爱学 (Doubao Ai Xue) — Native iOS + macOS Reimagining
 Agent Progress Log
==================================================================

PROJECT OVERVIEW
----------------
Goal: A native SwiftUI app for iOS 26 + macOS 26 that reimagines ByteDance's
豆包爱学 K12 AI-learning app — same surface area but better integrated, more
intuitive, optimized, and enhanced with Apple-platform intelligence.

Toolchain: Xcode 26.3, Swift 6.2 (language mode 6.0), MainActor-default isolation.
Project: 豆包爱学.xcodeproj — single multiplatform target, PBXFileSystemSynchronized
root group (drop .swift files into 豆包爱学/ and they compile automatically).
Module name: DoubaoAiXue. Platforms: iphoneos, iphonesimulator, macosx.

KEY FILES
---------
  handoff.md          — authoritative continuity record (read this first)
  ARCHITECTURE.md     — architecture & layout blueprint (after research)
  PLAN.md             — atomic build plan (after research)
  RESEARCH.md         — research dossier (from research workflow)
  feature_list.json   — feature checklist with passes flags
  init.sh             — build/smoke harness (./init.sh, mac, ios, run-mac)

==================================================================
 SESSION LOG
==================================================================

[2026-06-15] Session 1 — bootstrap & research (branch: main)
  - Recon: confirmed Xcode 26.3 / Swift 6.2 / iOS+macOS 26 SDKs, synchronized
    file groups in the existing project.
  - Project tuning: Swift 6.0, dropped visionOS, bundle id linroger022.DoubaoAiXue,
    module name DoubaoAiXue, camera/mic/photo/speech usage strings.
  - Removed broken template files (unrendered placeholders); added App/ baseline.
  - GREEN BUILD verified: macOS + iOS Simulator both BUILD SUCCEEDED.
  - Launched research workflow (wf_3fbb8a3f-db8): 7 domain researchers + synthesis.
  - Wrote RESEARCH.md (874 lines, 67 features), ARCHITECTURE.md, PLAN.md,
    feature_list.json, docs/FOUNDATION_API.md.
  - Wave 0 Foundation (single-writer): design system, 22 SwiftData models,
    Intelligence layer (Mock + FoundationModels + RoutePolicy), Vision/Speech,
    ContentCatalog, persistence + seeding, planner/system, adaptive shell.
    GREEN both platforms; app launches.
  - Wave 1 (wf_e6efd277-1ff, 10 agents): Onboarding, Home, Solve, Tutor, 错题本,
    作文批改, 口算批改, Companion chat, 知识点讲解, Profile. Integrated + fixed
    (logs-exclusion, CompanionTheme isolation, PhotosPicker label inline).
    39 features passing; GREEN iOS + macOS; macOS app screenshot verified.
  - Launched Wave 2 (wf_a116d824-55e, 12 agents): KnowledgeGraph, Classroom,
    DocumentQA, Dictation, Vocabulary, Oral, Reading(翻译+古诗文), Drill, Reports,
    Search, Parent, App Intents.
  - Next: integrate Wave 2 → wire AppDestinations → green build → validate.

[2026-06-16] Session 2 — Wave 2 recovery, integration & completion (branch: main)
  - Found the Wave 2 background workflow had finished writing ~25 new feature files
    but left the build BROKEN. Recovered to GREEN (macOS+iOS) by fixing 7 distinct
    breakages, each verified by rebuild:
      1. Deleted a duplicate OralScenario.swift (conflicting value types); integrator
         had replaced it with a non-conflicting @MainActor extension.
      2. Marked Oral static data (turns/avatars/corrections) `nonisolated`.
      3. PendingIntentStore: store appGroup String? (Sendable), resolve UserDefaults inline.
      4. StudyAppIntents: dropped type-level `nonisolated` (conflicts with @Parameter);
         intents are MainActor (implicitly Sendable), provider still constructs them.
      5. Added `import SwiftData` to 4 preview files (.modelContainer).
      6. DrillHandwritingInput: `#if canImport(PencilKit)` → `#if os(iOS)`/`canImport(UIKit)`
         (PKCanvasView/UIViewRepresentable are UIKit-only; module imports on macOS too).
      7. DocumentSupport: Result<_,String> → DocumentParseError (ExpressibleByStringLiteral);
         removed a duplicate SeededGenerator in TutorBlackboard (shares Vocabulary's).
  - Wired the AppDestinations seam fully: every section/route/tool(16, exhaustive)/sheet
    now resolves to a real view (DocumentQA, KnowledgeGraph, Reports, Dictation, Vocabulary,
    Oral, Translation, Classical, Drill, CourseDetail, WordDeck, DictationDetail,
    DocumentDetail, ParentMode, Search).
  - Wired App-Intent/Siri/Spotlight deep-link consumption: AppShell consumes
    PendingIntentStore on .task + scenePhase→.active → AppRouter.handle(_:regular:). (F55b)
  - Built the last 2 missing views with 2 PARALLEL subagents (isolated DerivedData):
      • F25 RecognizeAnythingView (识万物 — Vision VNClassifyImage/RecognizeAnimals + OCR
        + kid-friendly explanation, camera/photo/file, both platforms).
      • F53 AccountView (账号登录 — Sign in with Apple + 游客模式, @AppStorage db.account.*).
    Both returned green; integrated green.
  - Wired AccountView + a 每日提醒 notifications toggle (NotificationService) into
    ProfileSettingsCard (completes F13b).
  - Verified pre-existing F14/F45 (DrillView 今日靶向练习), F43 (CustomLessonSheet),
    F49 (Onboarding 教材版本 → LearnerProfile.editions) functional.
  - Integrated build GREEN on macOS + iOS (zero errors). Launched the macOS app: it runs
    without crash and renders the real Home + adaptive sidebar (screenshots saved to /tmp).
  - feature_list.json: all 50 features passes:true.
  - Tests run: xcodebuild macOS + iOS Simulator (iPhone 17 Pro) → BUILD SUCCEEDED both;
    macOS app launch smoke test → renders real UI, no crash.
  - Known follow-ups: optional UI polish pass; run macOS code-review; git commit (work is
    currently uncommitted on main — branch first per repo convention).
  - Committed the full app (50 features) and pushed: created PUBLIC GitHub repo
    github.com/linroger/doubao-aixue (account linroger via gh keyring), fast-forwarded
    main, pushed origin/main. .gitignore now also excludes .claude/ + .remember/.

[2026-06-16] Session 2 (cont.) — Multi-provider AI integration (branch: main)
  - User asked to incorporate multiple AI providers (Qwen/Doubao/MiniMax/Claude/Gemini/
    OpenAI/GLM/Kimi/DeepSeek) and wire models throughout the app.
  - New Services/Intelligence/Cloud/ layer:
      • AIProvider.swift — 9-provider catalog, 3 dialects (openAI-compat/anthropic/gemini).
      • KeychainStore.swift — encrypted API-key storage (sandbox-safe).
      • AICredentialStore.swift — @Observable source of truth (db.ai.* + Keychain) +
        ResolvedAIConfig + IntelligenceFactory.
      • CloudChatClient.swift — URLSession client for all 3 dialects + test().
      • CloudIntelligenceService.swift — implements the full IntelligenceService against
        the chosen model (real cloud for solve/tutor/chat/essay/explain/summarize/docQA/
        similar/lesson; deterministic graders local; every method falls back to offline on
        any error).
  - Wiring: DoubaoAiXueApp holds @State AICredentialStore, injects it, seeds
    \.intelligence via IntelligenceFactory.make(aiStore.resolved) → reading .resolved in the
    scene body re-binds the WHOLE app to the chosen provider on change. Every feature already
    uses @Environment(\.intelligence), so models are wired throughout automatically.
  - Added ENABLE_OUTGOING_NETWORK_CONNECTIONS=YES to both build configs (app was sandboxed
    with no network entitlement → cloud calls would fail on macOS at runtime).
  - Settings UI: Features/Profile/AISettings/AISettingsView.swift (provider grid + model
    picker + Keychain key field + 测试连接), built by a parallel subagent; wired into
    ProfileSettingsCard via a new 智能 → "AI 模型" NavigationLink row (live statusSummary).
  - Consulted the claude-api skill for correct Anthropic model IDs + Messages API shape.
  - Tests run: xcodebuild macOS + iOS Simulator (iPhone 17 Pro) → BUILD SUCCEEDED both;
    macOS app launch smoke test → renders Home + sidebar, no crash (new AI startup path runs).
  - Fixed during integration: json() helper calls needed `try await` (was `try`).

[2026-06-16] Session 3 — Integration pass: interlinking, bug fixes, AI everywhere (branch: main)
  Goal: make the app feel like one integrated whole, kill all issues, ensure AI is wired throughout.
  Method: 3 parallel Explore agents audited (A) AI wiring, (B) cross-feature links, (C) bugs; then a
  single-writer implementation pass with a green build after every increment.

  1) Warnings → ZERO. Made the design-system value tokens `nonisolated` (Color/Font/DBSpacing) so
     they read cleanly from SwiftUI's Sendable PhotosPicker label closures — cleared 17 main-actor
     warnings at the root. Replaced deprecated `Text + Text` in EssayFeedbackView with an
     AttributedString build. Replaced deprecated `UIScreen.main.scale` in DictationHandwritingSheet
     with the canvas's `traitCollection.displayScale`.

  2) KEYSTONE interlink — targeted practice. New `Route.drill(knowledgePointID:)` + `router.openDrill`;
     DrillView(targetKnowledgePointID:) → DrillModel.configure(preselected:) synthesizes/selects the
     target. Wired so 今日靶向练习 (Home), a mistake's 举一反三 (MistakeDetail), a report's 专项练习
     (Reports), and a knowledge point's 去练习 (KnowledgePoint) all launch practice ALREADY focused on
     the right knowledge point (previously they opened the generic picker — a dead feeling).

  3) Discovery links. KnowledgePointView gained a 典型错题 section (queries MistakeItems for the point →
     links to .mistakeDetail). DocumentDetailView gained a 让豆包老师精讲 card → tutor, closing the
     document dead-end. MistakeDetail KP chips now show the point NAME (was raw id).

  4) AI wired throughout (closed the only 2 gaps the audit found). TranslationView (课文翻译) and
     ClassicalStudyView (古诗文赏析) now consult the configured cloud model via intelligence.chat()
     when `capabilities.route == .cloud`, falling back to the deterministic offline engine otherwise
     (Translation parses per-sentence alignment defensively; route badge reflects cloud vs on-device).
     Every AI-capable feature now flows through the IntelligenceService seam.

  5) Bug fix. TutorSessionModel.askFollowUp's streamed follow-up Task was untracked → not cancelled on
     tearDown()/retry() (leak + TTS restart after the view is gone). Stored as `followUpTask`, cancelled
     in cancelEverything(), and guarded with Task.isCancelled. Also cleaned a convoluted force-unwrap
     (DictationDetailView title) and an unsafe `poems[0]` preview index (ClassicalView).

  Tests run: xcodebuild macOS + iOS Simulator (iPhone 17 Pro) → BUILD SUCCEEDED both, ZERO warnings;
  macOS app launch smoke test → process runs, renders real UI. All changes committed + pushed.

[2026-06-16] Session 4 — New features + parallel-build integration + macOS release (branch: main)
  Goal: keep making the app feature-rich/amazing; ship a ready-to-run macOS .dmg.

  My additions:
   - 成就 (Achievements): first built my own (Profile/Achievements), then discovered a parallel build
     process had added a RICHER Features/Achievements (XP/level ring + 连续打卡 heat-strip + tiered
     bronze/silver/gold/platinum badge wall + persisted unlock dates + celebration). Resolved the
     resulting duplicate-symbol collision by DELETING mine and re-pointing my wiring (Route.achievements
     + a Profile entry card) to theirs.
   - macOS menu bar + keyboard shortcuts (App/StudyCommands.swift, #if os(macOS)): 学习 menu
     (拍照解题 ⌘N · 批改 ⇧⌘G · 今日练习 ⌘T · 问豆包 ⌘L · 搜索 ⌘F) + 前往 menu (sections ⌘1–⌘9 + 成就),
     wired to AppRouter. Verified live: menu bar shows 学习 / 前往.

  Integrated a batch of features a parallel process left UNWIRED / not-quite-compiling:
   - Fixed builds: CalculatorView missing `import SwiftData`; FocusTimerView missing `import Combine`;
     ExamModel had `grade()` colliding with its `grade` GradeLevel property → renamed to `gradePaper()`.
   - Wired 5 complete, self-contained views as new ToolKind cases (auto-surface in 工具 hub + Search +
     AppDestinations.toolView): today (今日 daily planner), calculator (科学计算器+公式库),
     focus (专注·番茄钟), liveScan (实时扫题, iOS VisionKit + mac fallback), achievements.
   - Wired TutorVoiceSettingsView into the 豆包老师 toolbar (sheet): accent/dialect + 语速 + 试听;
     the model already narrates via voiceAccent.languageTag, so this completes a real feature.
   - Left LATENT (compile-clean, future work): Features/Exam/ExamModel.swift (no view yet — did not
     build one to avoid colliding with the still-active parallel process); Reports/ReportShareCard.swift
     (ReportsView already shares text).

  Release (macOS):
   - scripts/package_dmg.sh (Release build → ad-hoc deep-sign → hdiutil .dmg with /Applications symlink),
     scripts/notarize.sh (Developer-ID sign + hardened runtime + notarytool submit/staple — for the user's
     paid account), RELEASE.md (install + notarization gap). dist/ + *.dmg gitignored.
   - NOTARIZATION GAP: this Mac has only an Apple Development cert (team X8AD8YC886) + no notary creds.
     Apple notarization needs a Developer ID Application cert from a paid ($99/yr) account. Shipped an
     ad-hoc-signed .dmg (right-click→Open) + the one-command notarize.sh for when creds exist.
   - feature_list.json: 60 features, all passing.
   - Tests: xcodebuild macOS + iOS Simulator (iPhone 17 Pro) → BUILD SUCCEEDED both; macOS menu/UI verified.

================================================================================
SESSION (2026-06-17) — 作业批改 (workbook grading) + 题库 (question bank) + AI vision + audit fixes
================================================================================
Feature work (all reachable, mock-backed, all states handled, iOS+macOS BUILD SUCCEEDED, 0 warnings):

- F67 作业批改 (workbook grading): photograph / pick / upload a workbook page → AI grades every
  question into a structured, subject-agnostic GradedWorkbook (Models/WorkbookGradingTypes.swift:
  GradeVerdict, WorkbookQuestionType, GradedQuestion, GradedWorkbook). Rendered by
  Features/Workbook/WorkbookResultView.swift (score ring + tally + per-question ✓/✗/◐ cards with
  student-vs-correct answer, explanation, 错因, 知识点 chips, steps, rubric, teacher note; save
  wrong→错题本/题库, 讲一讲→tutor). Capture surface WorkbookGradingView (camera iOS / Photos / file
  importer both platforms) + WorkbookGradingModel (OCR pre-pass → gradeWorkbook → persist).
- F68 批改历史: WorkbookGradeRecord persists photo + GradedWorkbook JSON + metadata;
  WorkbookHistoryView lists + re-renders any past grading offline; delete via context menu.
- F69 题库 + F70 智能出题: BankedQuestion model; QuestionBankView (filter/star/reveal/delete) +
  QuestionBankModel (select → similarProblems grouped by subject → GeneratedPracticeSheet with
  attempt/reveal + 加入题库). Save-to-bank wired from WorkbookResult + SolveResult.
- F71 多模态视觉: CloudChatClient.completeVision for all 3 dialects (openAI image_url / anthropic
  base64 image / gemini inline_data) + AIProvider.supportsVision (openai/anthropic/gemini=true).
  CloudIntelligenceService.gradeWorkbook uses vision when supported else OCR-text; solve() now sends
  the captured image too. EVERY path falls back to the deterministic offline engine (never throws to UI).
- gradeWorkbook added to IntelligenceService protocol + all 3 impls (Mock real arithmetic + rich
  multi-subject sample; FoundationModels delegates+restamps; Cloud parses JSON→GradedWorkbook).
- Wiring: ToolKind.gradeWorkbook + .questionBank (4 switches in AppEnums + AppDestinations.toolView),
  Home quick-tools, macOS 学习 menu (作业批改 ⇧⌘G, 口算批改, 题库 ⇧⌘B). Real camera capture added to
  CaptureSolveView (was a photo-picker stub). SampleData seeds 4 题库 questions.

Audit fixes (from a 34-agent parallel audit; confirmed-real items only):
- CloudIntelligenceService.extractJSON: balanced brace/bracket scan with string+escape tracking
  (was fragile first-{/last-}). FoundationModelsService.chat re-stamps route to match the badge.
- DrillAnswerChecker + ExamAnswerChecker: token/whole-part matching (fixed "10" wrongly matching
  "x=5,y=10"). SolveResultView: savedToMistakes set only after a successful save; +收藏到题库.
- EssayGradingView: 升格范文 unlock now GATED on ParentControls.verified (+onChange) — closed the
  learn-mode bypass that revealed it unconditionally. DocumentQAView.ingest: aborts (deletes entity,
  error haptic) if the initial save fails instead of summarizing a never-persisted doc.
- ConversationView: streaming Task stored + cancelled .onDisappear + Task.isCancelled guards (no
  post-dismiss SwiftData mutation). ModelContainerFactory: graceful in-memory fallback with logging +
  clear fatalError instead of try!. App-wide: try? context.save() → context.saveLogging() (logs via
  OSLog) at ~26 feature-layer sites (Services/Persistence/PersistenceLog.swift). Photo-pick failures
  now give a warning haptic (Arithmetic/Essay/Translation/Workbook) instead of silent no-op.

Build infra side-quest: the working tree referenced LessonContentCatalog (a half-finished parallel
feature) that was never generated → clean build broke. Made scripts/generate_lesson_catalog.py tolerant
of an absent subject file + fixed its swift_string (was emitting invalid single-line """ literals);
regenerated Services/Catalog/LessonContentCatalog.swift = 69 valid scripted lessons. social.json still
absent (SampleData falls back to MockContent for those buckets).

Tests: xcodebuild macOS + iOS Simulator (iPhone 17 Pro) Debug → BUILD SUCCEEDED both, 0 warnings.
feature_list.json: 71 features, all passing.

================================================================================
Session 6 (2026-06-17) — pareto-optimization loop: audit → fix → review
================================================================================
Branch: main. Commits: b849811, 901d4ed, 1a4dd3c (on top of d2a3ab7).
Process: ran a 32-agent comprehensive audit (12 areas + adversarial verify) →
17 confirmed P0/P1 + 30 P2/P3. Implemented in waves, built green after each, then
a 6-dimension adversarial change-review on the diff. Both platforms BUILD SUCCEEDED,
0 warnings; macOS app launch-smoke-tested (SwiftData migration for new ActivityLog.count
+ new models boots clean).

HEADLINE: 答题足迹 GitHub-style daily contribution heatmap (the explicit ask) —
- ActivityLog gains a question `count`. New Services/Persistence/ActivityRecorder.swift:
  ActivityRecorder.log(kind/subject/questions/minutes) is the ONE funnel; ContributionStats
  (questionsByDay / dailySeries / currentStreak / bestDay) aggregates it.
- DesignSystem/Components/DBContributionGraph.swift = reusable GitHub-style heatmap
  (5-level green scale, calendar-week columns w/ current week last, future days faint,
  month+weekday labels, horizontal scroll anchored to today, a11y per cell) + legend.
- Features/Reports/ContributionCard.swift surfaced in 个人中心 / 今日 / 学习报告.
- Question counting wired through EVERY learning surface: 作业批改, 拍题解题, 靶向练习,
  模拟测验, 听写, 背单词, 口算批改, 作文批改, 智能出题练习 (all idempotent).
- Streak DERIVED live from ActivityLog (ContributionStats.currentStreak) across
  首页/今日/个人中心/成就 — no more frozen seeded "7 天". SampleData seeds ~12 weeks so
  the heatmap is alive on first run.

OTHER FIXES (all 17 confirmed P0/P1):
- 题库 subject-mislabel fixed (GeneratedProblem.subject stamped by Mock+Cloud); re-banking
  carries KP+explanation; full 题库 review loop (forgetting-curve self-rating + 今日复习 filter).
- 错题本 → 题库 (detail + bulk, BankedQuestion.make(from: MistakeItem)); 靶向练习 → 错题加入错题本.
- Non-math mock drills now WINNABLE (real A–D choice items, not 见解析).
- 模拟测验 revived from orphaned ExamModel → new Features/Exam/ExamView.swift + ToolKind.exam.
- iPhone gains persistent AI 伙伴 tab; detailPath resets on sidebar switch (iPad/Mac stale-detail bug);
  个人中心 历史记录→new SolveHistoryView, 收藏→题库 (3 distinct destinations); KG 去练习 targeted.
- ToolsHubView finished (category tints via ToolKind.tileTint, icons/subtitles, tokens, search);
  deleted dead ComingSoonView + Wave0Welcome; TTSService.isSpeaking resets on narration end;
  豆包/通义千问/智谱 gain vision models + supportsVision; macOS 学习 menu rounded out.

Gotcha added: Timer.publish(...).autoconnect() needs `import Combine` (bit ExamView).
